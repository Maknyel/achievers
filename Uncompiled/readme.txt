database config:
	hostname:localhost
	username:root
	password:
	database:achievers

	*if you want to change database config:
		-go to application/config/database.php
			-change hostname, username, password, database based on user database config

current baseurl: http://localhost/achievers/
	*if you want to change baseurl:
		-go to application/config/config.php and change base_url value (based on your port/ hostname)